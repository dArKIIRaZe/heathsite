{
  "name": "heathsite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "deploy:build": "npm run build && echo Build complete! Ready for deployment. Upload contents of 'out' directory to Plesk.",
    "deploy:check": "node -e \"const fs = require('fs'); const out = './out'; if (!fs.existsSync(out)) { console.error('❌ out/ directory not found. Run npm run build first.'); process.exit(1); } const files = fs.readdirSync(out); console.log('✅ Build found with', files.length, 'items'); console.log('📁 Main files:', files.slice(0, 10).join(', '));\""
  },
  "dependencies": {
    "axios": "^1.13.2",
    "cheerio": "^1.1.2",
    "fs-extra": "^11.3.2",
    "glob": "^13.0.0",
    "next": "16.0.7",
    "puppeteer": "^24.32.1",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "use-script": "^3.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.0.7",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
